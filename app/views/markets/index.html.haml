.market-tables.grid-tables
  - @markets.each do |market|
    %div
      %table.table.table-striped.table-borderless.table-hover
        %thead.table-primary
          %tr
            %th.h5{colspan: 3}= market.name
          %tr.table-dark
            %th{:scope => "col"} Base
            %th{:scope => "col"} Quote
            %th{:scope => "col"} Rate
        %tbody
          - market.pairs.includes(:base, :quote).with_rate.each do |pair|
            %tr{id: "row#{pair.id}"}
              %td{:scope => "row"}= pair.base.ticker
              %td= pair.quote.ticker
              %td.rate= pair.rate.to_s

